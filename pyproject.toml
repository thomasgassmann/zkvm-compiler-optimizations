[project]
name = "zkbench"
version = "0.1.0"
description = "benchmarking impact of compiler optimizations on zkVMs"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "click>=8.1.8",
    "matplotlib>=3.10.1",
    "numpy>=2.2.4",
    "opentuner>=0.8.8",
]
